Ты — ведущий fullstack-разработчик. Работаем в проекте Business Case (Next.js App Router + TypeScript + Tailwind + shadcn/ui + Prisma + Neon/Postgres + Auth).

ЗАДАЧА
Сделай главную страницу Business Case: два раздела со списком публичных кейсов:
1) Новые — сортировка по createdAt desc
2) Популярные — сортировка по количеству лайков desc
На главной показываем только PUBLIC кейсы. Без авторизации нельзя ничего править (создавать/редактировать/удалять/переключать private/public). Гость может только смотреть и (если по требованиям проекта) лайкать — но редактирование/CRUD строго запрещены.

ТРЕБОВАНИЯ К UI
- Верхнее меню (Header):
  - Лого/название Business Case (ссылка на “/”)
  - Навигация: Главная, Каталог, Мои кейсы
  - Справа: кнопка Войти/Выйти (в зависимости от сессии), мини-аватар, имя (если есть)
- Нижнее меню (Footer):
  - Короткий текст: (с) Business Case + год
  - Ссылки: Политика, Контакты
- Главная страница:
  - Hero-блок: заголовок + подзаголовок + кнопка “Добавить кейс”
    - Если НЕ авторизован: кнопка ведёт на /auth/signin (или ваш путь логина) и показывается подсказка “Войдите, чтобы добавлять”
  - Два блока списков:
    - “Новые” (последние 10–20)
    - “Популярные” (топ 10–20 по likesCount)
  - Карточка кейса в списке:
    - Заголовок, автор (имя), дата
    - Теги (если есть)
    - Кнопка “Открыть” (на страницу кейса /prompts/[id])
    - Счётчик лайков + кнопка лайка (если лайки уже реализованы)
    - НИКАКИХ кнопок “Редактировать/Удалить” на главной
- Адаптивность: красиво на мобильном.

ТРЕБОВАНИЯ К ДАННЫМ
- Сделай 2 запроса к БД (или один с двумя выборками) через Prisma:
  - recentPrompts: where { isPublic: true } orderBy { createdAt: "desc" } take N
  - popularPrompts: where { isPublic: true } orderBy по likesCount desc take N
- Для списка выводи likesCount через Prisma _count (или заранее посчитанное поле).
- Если пользователь авторизован — для каждой карточки добавь likedByMe (нужно для состояния кнопки лайка).
  - Реализуй это эффективно: один запрос по promptIds к таблице Like, затем мапа Set(promptId).

АРХИТЕКТУРА
- Создай общий Layout:
  - app/layout.tsx — подключи Header/Footer
  - Header/Footer как отдельные компоненты в components/layout/
- Главная страница:
  - app/page.tsx — Server Component
  - Получает session (если есть) и данные из Prisma
  - Рендерит два списка
- Компоненты:
  - PromptCard (server-safe или client-only частично)
  - LikeButton — client component (если лайки есть)
  - Sort/переключатели на главной НЕ нужны: это два фиксированных раздела.

ПРАВА ДОСТУПА
- Создание/редактирование/удаление кейсов и переключатель private/public должны быть доступны только авторизованным пользователям на соответствующих страницах (например /my).
- На главной и в публичном каталоге никакого редактирования.
- Если на проекте есть страницы редактирования — добавь guard: если нет session > redirect на login или 403.

ЧТО НУЖНО СДЕЛАТЬ
1) Реализуй Header/Footer и подключи в общий layout.
2) Реализуй главную страницу с двумя секциями “Новые” и “Популярные”.
3) Подтяни данные из Prisma: public prompts + likesCount (+likedByMe при наличии сессии).
4) Проверь, что гость не видит кнопок редактирования и не может открыть страницы редактирования напрямую.
5) Покажи список изменённых файлов и ключевые фрагменты кода.

ПРИМЕЧАНИЯ
- Используй shadcn/ui компоненты: Card, Button, Badge, Separator, DropdownMenu (для профиля), Skeleton (опционально).
- Не ломай существующий функционал.
- Если структура модели отличается (например поле public называется visibility), сначала найди актуальные названия в prisma.schema и подстрой код.
