Ты — senior full-stack разработчик. Сделай в моём проекте Business Case (Next.js App Router, TypeScript, Tailwind, shadcn/ui, Prisma, NeonDB/Postgres) Личный кабинет с интерфейсом как на референсе:

UI (как на картинке):
- Двухколоночная компоновка:
  1) Слева — вертикальный сайдбар шириной ~280px на светло-голубом фоне с градиентом/легкой текстурой.
     Вверху: круглый аватар, рядом/под ним имя пользователя (пример: “Антон Л.”).
     Ниже пункты меню с иконками:
       - Промты (active подсветка)
       - Избранное
       - История
       - Настройки
     Активный пункт выделяется плашкой/подсветкой.
  2) Справа — основная область контента на белом фоне:
     Заголовок H1: “Личный кабинет”
     Подзаголовок H2: “Мои кейсы”
     Ниже список карточек кейсов:
       - слева маленькая иконка “чат/бабл”
       - далее: жирный заголовок кейса (title)
       - ниже: серым короткий текст кейса (preview, ограничение 1–2 строки)
       - справа: иконка звезды (избранное) и ряд маленьких action-иконок (редактировать, удалить, переключить public/private)
     Карточки со скруглением, тонкой рамкой и легкой тенью.

Функциональность (CRUD + публичность):
- Сущность Prompt:
  id, userId, title, content, isPublic (boolean), isFavorite (boolean), createdAt, updatedAt
- “Мои кейсы”:
  - Create: кнопка “+ Новый кейс” открывает модалку/диалог с формой (title, content, toggle public/private).
  - Read: список только текущего пользователя, с пагинацией (по 10) и поиском по title/content.
  - Update: редактирование в диалоге (title, content, isPublic).
  - Delete: удаление с подтверждением.
  - Toggle public/private: быстрый переключатель прямо на карточке (или в меню действий).
  - Toggle favorite: кликом по звезде.
- “Публичные кейсы” (в этом же кабинете как вкладка/страница):
  - показывает все prompts где isPublic = true
  - карточки без кнопки удаления (если не владелец)
  - если владелец — доступны edit/delete как в “Мои кейсы”

Технические требования:
1) Маршрут кабинета:
   - /dashboard — “Мои кейсы”
   - /dashboard/public — “Публичные кейсы”
   - /dashboard/favorites — “Избранное”
   - /dashboard/history — “История” (пока заглушка)
   - /dashboard/settings — “Настройки” (пока заглушка)
2) Авторизация:
   - Предположи, что auth уже есть (например, NextAuth) и доступен userId на сервере.
   - Сделай серверные проверки: пользователь может изменять/удалять только свои prompts.
3) Реализация данных:
   - Prisma schema + миграция
   - Серверные actions (или route handlers) для CRUD:
     createPrompt, updatePrompt, deletePrompt, togglePublic, toggleFavorite
   - Валидация входных данных через zod
4) UI-компоненты:
   - Sidebar: отдельный компонент DashboardSidebar
   - Cards list: PromptCard
   - Form dialog: PromptDialog (shadcn/ui Dialog + Form)
   - Search input + debounce
   - Empty state: “У вас пока нет кейсов — создайте первый”
   - Loading state и оптимистичные обновления (где уместно)
5) Стили:
   - Tailwind + shadcn/ui
   - визуально близко к референсу: мягкие скругления, аккуратные отступы, спокойные цвета, без кислотных акцентов
   - используем lucide-react иконки: MessageSquare, Star, Pencil, Trash2, Globe/Lock, Settings, History, Bookmark
6) Качество:
   - Разбей на небольшие файлы, понятные имена
   - Добавь комментарии в ключевых местах (проверка прав, серверные action’ы)
   - Никаких “TODO” без необходимости — лучше заглушки с текстом “Скоро…”

Что нужно сделать:
- Создай/обнови prisma/schema.prisma под Prompt и миграцию.
- Сделай страницы и компоненты кабинета.
- Подключи CRUD через server actions (предпочтительно) или route handlers.
- Дай краткую инструкцию: какие ENV нужны (DATABASE_URL), как запустить миграцию и проверить.

В конце выведи:
- список созданных/измененных файлов
- как проверить функциональность по шагам (создать, отредактировать, удалить, переключить public/private, избранное)
